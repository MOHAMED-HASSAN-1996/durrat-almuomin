org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:ReservedCodeCacheSize=512m
android.useAndroidX=true
kotlin.incremental=false
# Disable daemon to prevent stale state causing DEX hangs
org.gradle.daemon=false
# Disable parallel to avoid resource contention
org.gradle.parallel=false
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Increase Gradle network timeouts for reliable artifact resolution
systemProp.org.gradle.internal.http.connectionTimeout=180000
systemProp.org.gradle.internal.http.socketTimeout=180000
