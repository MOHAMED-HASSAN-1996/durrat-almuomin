# إصلاح الأذان والتنبيهات — التصميم الحالي

## المطلوب
1. **تنبيه أول**: «اقتربت الصلاة — أقم صلاتك» قبل الأذان بعشر دقائق.
2. **تنبيه ثانٍ**: الأذان نفسه عند موعد الصلاة، ولا يتأثر بأي شيء في التطبيق —
   لا يتوقف إلا لما **المستخدم يلغيه بنفسه**.

## المشكلة القديمة
- إشعار الأذان كان يُلغى من كود التطبيق نفسه: عند إعادة جدولة النافذة، وعند
  إعادة تشغيل التطبيق، وعند تشغيل معاينة، وعند تغيير الإعدادات. يعني أذان
  شغّال بالضبط بيُسكَت من سطر `cancel(id: notifId)` عند أول فتح للتطبيق.
- كان يعتمد على **Full-screen Intent** لتشغيل الشاشة؛ لما النظام يرفض الصلاحية
  يمر الإشعار صامتًا بلا شاشة وبلا صوت.
- إعدادات الإشعار كانت متناقضة: `autoCancel: true` مع `ongoing: true`،
  ومعرّف قناة متغيّر (`v4`, `v7`, `v9`) مع بقايا قنوات قديمة على الأجهزة.
- تنبيه «اقتربت الصلاة» كان متناثر التعريف في ثلاثة أماكن مختلفة.

## التصميم الجديد

### 1. سجل ملكية الأذان (adhan slot registry)
`Map<int, DateTime> _adhanSlots` يسجّل: الإشعار رقم كذا ملك لأذان وقته كذا.

- `_setAdhanSlot(id, time)` — يُسجَّل قبل كل جدولة، ويُلغي الإشعار القديم **إلا**
  إذا كان الأذان الحالي شغّالاً فعلاً.
- `isAdhanActive(id)` — صحيح إذا مرّ أقل من `_adhanGraceMinutes = 20` دقيقة على
  وقت الأذان. خلال هذي النافذة **لا يستطيع أي مسار في التطبيق إلغاء الإشعار**.
- النتيجة: إعادة تشغيل التطبيق وأنت تسمع الأذان ما تُسكِت الأذان؛ ولا تُنسى
  إشعارات باقي اليوم أو الأيام القادمة.

### 2. إشعار الأذان الدائم
```
playSound: false            // يستعمل صوت القناة adhan.mp3
enableVibration: true       // مع adhanVibrationPattern
ongoing: true               // غير قابل للسحب/الإزالة
autoCancel: false           // لا يُلغى عند الدوس عليه
onlyAlertOnce: true         // لا يُعيد التنبيه عند التحديث
timeoutAfter: 5 دقائق       // يتوقف الصوت لحاله بعد مدة ملف الأذان
actions: [إيقاف الأذان]      // cancelNotification: false — الإلغاء يتم من الهاندلر
fullScreenIntent: false     // لا يعتمد على أي صلاحية إضافية
```
**السلوك**: الصوت يشتغل مرة واحدة، والإشعار يبقى على الشاشة لحد ما المستخدم
يضغط «إيقاف الأذان» أو يدوس على الإشعار.

### 3. مسارات الإسكات
| الحدث | يوقف الصوت | يحذف الإشعار |
|---|---|---|
| زر «إيقاف الأذان» / الدوسة على الإشعار | ✅ | ✅ (بطلب المستخدم) |
| زر الصوت (تعلية/خفض) | ✅ | ❌ |
| انتهاء مدة الملف / `timeoutAfter` | ✅ | ❌ |
| إعادة جدولة النافذة أو إعادة تشغيل التطبيق | ❌ | ❌ (الأذان الشغّال) |
| تشغيل معاينة | ❌ | ❌ (إلا معاينة سابقة) |

### 4. تنبيه «اقتربت الصلاة»
- قناة مستقلة `adhkar_pre_prayer_v5_reminder_channel` بصوت `iqtarabat`.
- نبضة اهتزاز خفيفة مختلفة (`prePrayerVibrationPattern`) حتى ما يُخطَأ بينه وبين
  الأذان.
- `autoCancel: true` — يزول عند الدوس عليه فقط، ولا يمسّ إشعار الأذان.
- يُجدول قبل الأذان بعشر دقائق لكل صلاة مُفعَّلة وللـ 30 يوماً القادمة.

### 5. تنظيف الأذانات القديمة
`cleanupStaleAdhanNotifications()` تُستدعى عند تشغيل التطبيق وعند الرجوع له
(الـ resume): تحذف إشعارات الأذان من أيام سابقة فقط، وتترك أذان اليوم الحالي
كما هو.

## البنية في الكود
- `lib/services/prayer_alert_service.dart` — كل منطق الأذان والتنبيهات:
  `_adhanNotificationDetails()`, `_prePrayerNotificationDetails()`,
  `_setAdhanSlot()`, `isAdhanActive()`, `stopAzanSound()`, `stopAzan()`,
  `cancelAdhanNotifications()`, `cleanupStaleAdhanNotifications()`.
- `lib/app.dart` — تنظيف الأذانات القديمة عند resume.

## ملاحظات للصيانة
- لا تضف `cancel(id: notifId)` للأذان إلا داخل `_setAdhanSlot` (المحمي)
  أو في `stopAzan()` (بطلب المستخدم).
- لا تُعِد `fullScreenIntent`/`AdhanAlertScreen` — التصميم الحالي لا يعتمد
  على أي صلاحية إضافية لكي لا يمر الأذان صامتًا.
- أي تغيير على صوت القناة يستلزم رفع رقم الجيل في `_channelId` (Android يحفظ
  إعدادات القناة القديمة).
