# قواعد العمل (AGENTS.md)

## قواعد الـ Git

1. **ممنوع الجلب من الـ git بدون إذن**: لا تسحب (pull)، ولا تزامن، ولا تستعيد (checkout)، ولا تجلب أي شيء من الـ git — إلا إذا طلب المستخدم ذلك صراحةً.
2. **رفع التحديثات**: عند وجود تحديثات/تغييرات غير مرفوعة على الـ git، قم بعمل commit و push تلقائيًا (ما لم يطلب المستخدم خلاف ذلك).
3. لا ترفع أبدًا ملفات الـ dump والأدوات المؤقتة الخاصة بالتصحيح مثل: `u.xml`، `ub.xml`، `ui*.xml`، `cd.txt`، ومجلد `android/.kotlin/`.
4. رسائل الـ commit باللغة العربية بنفس أسلوب الـ repo الحالي (مثل: `feat: تحديثات شاملة ...`).
5. لا ترفع أي مفاتيح/أسرار (secrets) في الـ git.